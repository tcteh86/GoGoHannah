{
  "name": "Python 3",
  // Or use a Dockerfile or Docker Compose file. More info: https://containers.dev/guide/dockerfile
  "image": "mcr.microsoft.com/devcontainers/python:1-3.11-bookworm",
  "customizations": {
    "codespaces": {
      "openFiles": [
        "README.md",
        "backend/app/main.py",
        "frontend/README.md"
      ]
    },
    "vscode": {
      "settings": {},
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ]
    }
  },
  "updateContentCommand": "[ -f packages.txt ] && sudo apt update && sudo apt upgrade -y && sudo xargs apt install -y <packages.txt; [ -f backend/requirements.txt ] && pip3 install --user -r backend/requirements.txt; echo '✅ Packages installed and Requirements met'",
  "portsAttributes": {
    "8000": {
      "label": "API",
      "onAutoForward": "openPreview"
    }
  },
  "forwardPorts": [
    8000
  ]
}